# Tasks

## 1. 创建 zdash skill
- [ ] 创建 `skills/zdash/SKILL.md`
- [ ] frontmatter：name=zdash、icon=📊、description 含触发词（打开面板/启动面板/拉起dashboard/看板）
- [ ] 正文：单条启动命令 + 复用(exit 0) + --restart + 端口 + #<mode> 深链
- [ ] 边界节：不写数据、不判空面板、止步浏览器
